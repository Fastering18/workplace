#include <bits/stdc++.h>
#include <iostream>
#include <string>

using namespace std;
//static const long long mod = 1e9 + 7;

int main() {
  int t;
  cin >> t;
  while (t--) {
    string s;
    int res = 0;
    cin >> s;

    while (res > 9) {
      for (int i=0; i<(int)(s.size()); i++) res += (int)(s[i]);
      s = (string)
    }
  }

  return 0;
}